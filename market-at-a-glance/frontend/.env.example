# Copy to .env.production.local (or set as a Vercel project environment
# variable) and point it at your deployed backend, including the /api
# suffix. Leave unset for local dev — vite.config.ts proxies /api to
# http://127.0.0.1:8000 automatically.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
